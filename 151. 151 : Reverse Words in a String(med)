class Solution:
    def reverseWords(self, s: str) -> str:
        a = s.split()
        arrlen = len(a)
        for i in range(2,arrlen+1):
            a.append(a[arrlen-i])
        return ' '.join(a[arrlen-1:])
        
       
